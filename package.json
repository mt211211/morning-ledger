{
  "name": "morning-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute morning-ledger --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute morning-ledger --remote --file=./schema.sql",
    "preview": "node local-preview.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.19.1"
  }
}
